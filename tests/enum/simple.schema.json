{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "the_enum",
  "description": "Some demo structure for demoing.",
  "type": "string",
  "enum": ["value1", "value to s@nitize", "and_the_last"]
}
